Program Test; 
   
Var a : Integer;
    b : Integer;
    c : Char;
(*Function F(b: Integer ; n : Integer) : Integer;
  var i: Integer;
      a : Integer;
  Begin
    a := 1;
    For i := 1 to n do
      Begin
        a := a * b;
      End;
    F := a;
  End; *)

Begin
  a := 1;
  b := 2;
  c := '3';  
  a, b, c:= 3 + b - 2**4, a-b%2, 'a';
  call writeI(a);
  call writeI(b);
  call writeC(c);
End. 